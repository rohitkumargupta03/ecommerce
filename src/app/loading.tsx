export default function Loading() {
  return (
    <section className="panel" style={{ marginTop: "1rem" }}>
      <h2 className="page-title" style={{ fontSize: "1.4rem" }}>
        Loading products...
      </h2>
      <p className="muted">Please wait while we fetch the latest catalog.</p>
    </section>
  );
}
